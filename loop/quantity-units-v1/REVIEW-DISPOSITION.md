# Astra review disposition

The independent reports are preserved verbatim as review evidence. Findings describe their input candidate, not necessarily the final implementation. Reviewers did not execute tests; the integration evidence is Astra's.

| Review | Disposition |
|---|---|
| SQL | Accepted and repaired unsafe version casts, the volume-unit whitelist, whole-record validity and food-page payload coverage. All 45 migrations replayed into an owned disposable database, followed by existing and new guards. No historical SQL changed. |
| Core B1–B4, M5, M7–M8, L10–L11 | Repaired unusable density, offered-unit validity, selected-serving nutrient coverage, outgoing invalid metadata isolation, JSON extensions, remembered-unit recosting, per-slot races, merge deletion state and overflow. Regression suites cover these paths. |
| Core M6 / surfaces M7 | Conservative unit identity is deliberate and agrees between SQL and client. Quantity precision and original units survive unchanged saves. A changed basis requires review. |
| Core M9 | The manual relation factory is a domain constructor, not automatic photo acceptance. Photo results remain proposals, and the editor requires explicit confirmation including unsafe-basis acknowledgement. |
| Core L12 | Unknown mass-display values fall back to Automatic as specified. The current server accepts only the three known values. Future enum expansion requires its own compatibility work. |
| Surfaces H1 | Rejected: two zero-macro serving rows do not establish physical equivalence. Do not invent density from their coexistence. Direct same-kind nutrition retains precedence and zero-calorie package conversion is tested. |
| Surfaces H2, M1–M2, M4–M5, M10, L2 | Repaired durable capture evidence, basket counts, shopping editor units/precision, duplicate pop, thumbnails/role and field provenance, unconfirmed-link removal, and bounded notes. |
| Surfaces M3 | Consolidation carries package evidence into the preferred unit before the combined row formats it. Raw hints are display evidence only; canonical quantities are not reparsed. Domain and step-surface tests protect this. |
| Surfaces M6 / repair R2 basis | Ordinary panels are requested as as_packaged; missing or ambiguous basis remains unknown and requires acknowledgement. Older label-only responses lacking package facts do not trigger the package-basis question. |
| Surfaces M8 | “Use this one” deliberately selects the already-saved duplicate. It does not overwrite that food with the draft. Existing duplicate-selection behavior is retained; confirmation alone does not commit a draft. |
| Surfaces M9 | The approved relationship is mass package to volume nutrition serving; reverse conversion means converting either direction through that same relationship. It does not require a second volume-package/mass-serving metadata type. |
| Surfaces L1, L4–L7 | No blocking defect verified. Final repository settlement reads current food; reactive unit preferences are intended. Existing repoint breadth, Reset behavior, and empty-photo wording are unchanged. |
| Surfaces L3 | Explicit boolean approximation is part of v1. Malformed v1 remains inactive; omission is not silently asserted exact. Unsupported records are retained without blocking unrelated sync. |
| Repair R1–R2 text | Added implied-conversion validation, an explicit confirmation failure message, and per-note encoded-byte/rune limits. Regression tests first failed on overflow feedback and Unicode truncation, then passed. |
| Planned-serving F1–F4 | Repaired recent repeat context, absent-serving shopping signal, selected-serving chip fallback, and null-food hardening. Added actual one-tap widget tests and repository/domain regressions. |
| Planned-serving F5 | Mandatory deployment order: SQL first, compatible function second, client release last. No client release or deployment has occurred at this checkpoint. |
| Planned-serving F6 | Recorded pre-existing risk: v28 alters shopping_list_items before the older creation step. This feature does not alter that ordering. Real historical schemas available for replay are v25–v28; interrupted-upgrade fixtures cover v1–v28. Those are different claims. |
| Visual 1–3 | Implemented clearer confirmation/Save wording, package preview including 1 cup = 5 oz, Weight display below package arithmetic, full-width large-text fields, previews and wrapping photo actions. Corrected two capture errors (mid-transition cook route and shopping list mistaken for amount sheet), rerendered and independently re-reviewed actual settled captures. No visual blocker remained in review 3's four-image scope. |

The planned-serving review ran before Astra's explicit-null companion and reopened approximation fixes; passing tests, not its earlier source inspection, establish those fixes. The final focused review checks the repaired repeat and confirmation paths. Native interaction, hosted migration parity, live two-photo extraction and CI are separate gates, reported in RESULTS.md.

Final Opus review: F1–F4 and confirmation repairs accepted with no blocker in its scope. O1 is a wording edge case for an existing food with no serving rows; O2 is a nullable API argument correctly supplied by its only caller; neither changes stored data. O3's explicit editor error handling and complete-record validation were inspected by Astra and exercised by draft/editor tests. No new repair is required. The report misread “interrupted-upgrade fixtures” as an interrupted test run: the v1–v28 fixture replay completed; true historical snapshots exist only from v25. QuantityMapper writes literal `mass`/`volume`; its unit IDs match the SQL whitelist (including pint/quart/gallon), resolving the reviewers' source-context uncertainty.
