# Execution decisions — approved plan v2

D15: Use a dedicated git worktree/feature branch and foreground supervised restricted Claude transport; no unrestricted worker capability is enabled. Supported transport records every actual model/session and limits, while Astra owns deterministic integration gates. See RUNTIME.md.

D16: Preserve mass authored hints in scaling/normalisation. Shared formatter accepts explicit food mode, known pack size and parsed package-unit context; generic automatic fallback preserves ounce-authored quantities. Combination picks deterministic source mass hints. Separate food wrapper supplies matched context to screens. Do not encode a render choice back into an authored quantity.

D17: New failing regression suite run before source changes: five expected failures (28 oz promotion, 14.5/15.25 ounce precision, 12+12 ounce combination, doubled canned ingredient hint). Three controls passed. Logs remain in private run directory and will be included in the verification summary.

D18: Database validation must use a fresh disposable database or CI fixture. Never reset the existing local hearth database, which may contain user data. The existing linked migration history was read only. Hosted additive migration stays behind local/review gates.

D19: Prepare the independent SQL candidate against the frozen R13 JSON contract while D1 generates domain code. Both Claude sessions are tool-less and cannot write the checkout. Astra remains the sole applying writer; SQL integration waits for M1. This scheduling adjustment does not change schema or release acceptance.

D20: Freeze additive label wire fields: package_amount/package_unit, servings_per_container, servings_approximate and package_basis (as_packaged/prepared/drained/unknown), alongside existing servings/uncertain. Independent server candidate preparation is allowed before UI integration; it owns no shared Dart path. At most three restricted Claude candidates plus Astra; one Astra writer and one heavy validation job remain.

D21: A new-client unrelated edit that resubmits the identical stored package record is permitted when an older app has made its source facts stale. New/changed records still validate membership and snapshots. Bound numeric JSON to finite double range. Opus repair acknowledged D21; new SQL regression failed on original candidate and passed after repair in the disposable database.

D22: Freeze LabelReading client additions as packageSize (Quantity?), servingsPerContainer (double?), servingsApproximate (bool), packageBasis (string enum). Draft owner consumes this interface while independent scan owner implements it. Prepare persistence, scan UI and draft candidates concurrently against the accepted domain interfaces; no overlapping writers, maximum three Claude sessions plus Astra, one heavy validation job.

D23: Reject automatic confirmation on Food Save. Use an explicit inline package review action; unrelated edits may preserve a stale record without reactivating it. Preserve exact source quantities and stable serving IDs in drafts. Manual changes to photo facts become mixed provenance. Partial optional metadata never prevents an ordinary food save.

D24: Shopping contributions retain authored source quantities and kinds. Convert only derived totals against current matched-food metadata, including read/export resolution; changing or removing a package relationship must recalculate an existing cup-based ask. Raw package syntax can restore a display hint without reparsing the canonical amount.

D25: Independent Opus SQL/core/surface reviews returned actionable issues. Repair invalid density precedence, selected-serving coverage, remembered display-unit recosting, per-slot photo cancellation, and invalid relationship outgoing payload isolation. Preserve future JSON extensions and original capture evidence. SQL membership and preferred-unit snapshots deliberately agree; an unknown unit or changed unit requires conservative re-review.

D26: Reject the surface review suggestion to infer density merely from two zero-calorie rows. Independent rows do not establish equivalent servings when all macros are zero; the direct same-kind nutrition path already precedes package conversion, and a confirmed package relationship works without energy. Do not invent physical equivalence. Existing explicit food density must survive editor saves.

D27: Actual Flutter captures revealed poor large-text photo-control wrapping. Separate selected-photo status, preview, and actions; make the sheet scrollable. Independent vision review requested separating Weight display from package arithmetic and clarifying inline confirmation versus Save; both accepted. One-shot delivery prevents a repeated done frame from popping two routes.

D28: macOS synthetic memory-database preview builds and launches. Native computer-use verification is currently blocked by the Mac lock screen; an unlock request is pending. No production database or installed app data is used for this fixture. Continue deterministic, image, and server checks; report the native gate truthfully unless it becomes available.

D29: The final path audit found that Plan only discarded the selected package nutrition serving, yielding 1,200 kcal on reopen after a 600-kcal preview when two cup rows differ. Add an optional planned-food serving reference so the stored count has an explicit basis. Legacy/default counts remain unchanged; logged snapshots remain frozen. Extend the unreleased local v29 schema and add a separate additive server migration; regenerate/replay both. New writes explicitly clear via an empty-string wire sentinel; the server preserves an existing reference when an older writer omits the field. This is necessary to satisfy the already approved R12 planned-food path, not a change to the product scope.

D30: Extend the selected-serving reference through recent-food repeats. A repeat resolves the current matching serving, preserves unknown nutrients, and refuses an absent serving rather than guessing from the default. A missing selected serving remains on the shopping list as an unquantified need. Independent review findings F1–F4 are accepted; repeat and missing-shopping regressions were observed failing before repair.

D31: Confirmation errors must be actionable. Guard overflowing/underflowing implied conversions before offering confirmation; bound capture notes by encoded bytes at Unicode code-point boundaries, preserving basis and field provenance. A failed confirmation still gives an explicit message. Keep unknown photo basis conservative; ordinary panels are explicitly transcribed as as_packaged by the server prompt.

D32: The Mac remains locked on a second native-tool attempt. Do not weaken the native walkthrough gate or infer a waiver from silence. Prepare a draft PR and finish all independent automated checks; keep M3/M4 and deployment pending until the native gate is resolved. This is a reviewable checkpoint, not a release.
