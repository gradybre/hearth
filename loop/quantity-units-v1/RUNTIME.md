# Foreground execution record

Approved plan v2; source baseline 4be6bc8; working branch fix/package-quantity-nutrition in /Users/brendan/Developer/hearth-quantity-units.

Astra supervises bounded Claude file-bundle sessions using the installed controller package’s verified no-tools startClaude transport. The transport validates pinned Desktop version, subscription authentication, no tools/MCP, expected model, deadline and owned process shutdown. No unrestricted tool worker, billed development API, validation fixture or unattended supervisor is used.

Scheduling is foreground and dependency-aware under Astra, with separate author/reviewer sessions; no service scheduler is active for this run. Packets, prompts, source hashes, actual model/session, owner/progress/results are stored in private /private/tmp/hearth-quantity-20260919. This stages real product work through the supported restricted transport; transport completion is not milestone acceptance. Astra validates paths/base hashes, applies tests first, runs red/green checks, and integrates candidates. Review and release gates in the approved execution package remain mandatory.

The wrapper enforces 15-minute total and 4-minute no-progress deadlines, invokes caffeinate only while an owned session runs, and records actual stopped ownership. Up to three independent tool-less Claude candidate sessions plus Astra; one checkout writer (Astra) and one heavy validation job. SQL candidate preparation overlaps domain generation only against the frozen JSON contract. Existing unrelated processes are untouched.

Preflight: Flutter 3.47.1 / Dart 3.13.1; GitHub authenticated; source and origin/main agree at 4be6bc8; linked Supabase local/remote migrations agree through 20260918150000; loopctl doctor passes development checks, productionReady=false/toolWorkersEnabled=false.
