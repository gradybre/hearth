# Execution decisions — approved plan v1

D-WALMART-001: Astra freezes the deterministic URL/result contract in the bounded worker input. Visibility is model transcription; server/client syntax checks do not prove image accuracy. Live extraction remains a separate gate.

D-WALMART-002: Large existing source files may be implemented by Claude as exact replacement bundles carrying the original full-file SHA-256. Astra validates allowed paths, base hashes, unique replacement anchors and applies them during integration. This avoids repeating thousands of unrelated lines in model output. A passing artifact-structure check is not product runtime evidence; final candidate runtime tests and independent reviews remain mandatory.

D-WALMART-003: The installed transport enforces byte limits rather than an exact output-token ceiling. Packet prompts bound intended output; process deadlines and a 240-second no-progress watchdog remain enforced. No broader tools or billed model APIs are enabled.

D-WALMART-004: Native CUA reported the Mac locked. An unlock request is pending. Continue source, deterministic checks and actual Flutter rendering; do not claim native interaction or live deployed extraction passed.

D-WALMART-005: The real-project service launched verified subscription-backed Sonnet session 3aa6a8e5-a67f-46eb-aab6-e44ee35afba9, but rejected invalid/double-escaped JSON output. Author owner and service stopped; no source applied. Recovery and subsequent bounded stages use the same installed controller's exported no-tools startClaude adapter under Astra foreground supervision, following the prior Hearth product runtime pattern. No competing service remains active. This preserves approved scope, separate reviewers, exact path/hash integration, 15-minute/4-minute deadlines, one Claude session at a time, and all acceptance gates. Service acceptance is not claimed for recovered work.
