# Execution package v2 — P-HEARTH-COOK-001

The user approved PLAN.md with the recorded font-size review feedback in APPROVAL.md; PLAN-v2.md is the execution baseline. Astra is sole planner/integrator. Maximum four model sessions including Astra, one heavy job, one writer per owned path. Controlled foreground execution only; reconcile owned processes before retries.

## C0 — configure and prove project execution (Astra)

Inputs: approved plan, baseline SHA, installed controller public contracts/docs and current capability gates. Recheck source status and running owners. Create a dedicated branch/worktree plus private state/evidence directories outside source/workspaces, using actual controller schemas. Publish approved plan with actual user approval reference; configure canonical source session grouping and supported structured author/reviewer route, contained Flutter checks and native capture/evidence boundary. Do not copy fixture identities or reuse bootstrap scripts as a product run. Record CLI/provider versions, actual IDs and scoped subscription check. No product writes before approval. If a capability is unavailable, retain plan and report exact gate; no billed fallback or invented controller commands.

Expected artifacts: current runtime configuration without exposed secrets, durable run/task identity and confirmed bounded ownership. Deadline 20 minutes; stop at unsupported integration rather than expanding controller product scope.

## C1 — tests and implementation (Claude Sonnet, one writer)

Requirements: R1–R6, acceptance A1–A6, milestone M1. Inputs: PLAN-v2.md hash, exact baseline commit, bounded copies of cook_along_screen.dart, step_amounts.dart, theme tokens, recipe/domain interfaces and matching/format contracts, relevant test helpers and cooking tests. Actual model/session ID must be captured from provider result before acceptance; unresolved IDs are not evidence.

Owned paths: lib/features/recipes/cook_along_screen.dart; lib/features/recipes/step_amounts.dart; test/features/recipes/cook_ingredients_layout_test.dart; test/features/recipes/cook_along_test.dart; test/features/recipes/cook_scaled_test.dart. A single test/render/cook_ingredients_render_test.dart may be added for repeatable focused captures. Existing shared gallery/helper registries belong to Astra, never the worker. All other paths forbidden unless Astra versions the packet.

Recipe: reproduce the 12-point focused ingredient defect with a failing regression; implement cook presentation with compact default; make _StepCard select it; eliminate empty spacing; address semantics; verify natural wrapping and readable scroll area. Any overflow in adjacent fixed controls triggered by the required matrix is repaired within cook_along_screen.dart only. Keep check/advance/timer callbacks and state semantics intact. Never edit generated files or alter formatter/matcher behavior.

Expected output: bounded structured file bundle/patch, changed-path list, actual model/session, consumed plan hash, uncertainties, expected tests, command evidence if the runtime executes them. Context limit: targeted source and interfaces, no unrelated repo history; output limit 16k tokens; attempt deadline 15 minutes, no-progress 4 minutes; at most two bounded repair attempts. Ambiguity returns to Astra with requirement ID and options before incompatible changes.

## C2 — deterministic and visual evidence (Astra, one heavy slot)

Depends C1. Inspect proposed paths/content before application. Record candidate hash. Prove regression failed on old UI and passed on new UI. Run existing targeted command from PLAN.md plus new layout test; then dart format --output=none --set-exit-if-changed ., flutter analyze, flutter test, and HEARTH_RENDER=1 flutter test test/render --tags render. Record real exit status; never infer from a piped tail.

Capture real Flutter screen images with loaded bundled fonts, synthetic ingredients and all specified viewports. Validate scroll reachability, safe areas, long instructions/eight ingredients, empty panel, scaled quantities, semantics and noninterfering advance/check/timer interactions. Run a native macOS fixture walkthrough if supported; retain direct evidence separately. Deadline 30 minutes per full suite; progress poll within 60 seconds; terminate only proven owned hung processes. A failing mandatory check goes to repair, not a relaxed tolerance.

## C3 — independent reviews (separate Claude sessions)

Depends exact C2 candidate and evidence. Code/security reviewer gets diff, relevant full methods, requirements and deterministic results; visual reviewer gets actual candidate Flutter captures and layout acceptance. No author session reuse. Routine review Sonnet; escalate substantial security/authority issue to Opus only if needed. Record actual IDs, exact candidate, methods, findings and disposition. Read-only, no write lease. Output <=6k tokens per review, deadline 10 minutes, no progress 4 minutes, at most two review/repair cycles. Visual evidence transport must be a proven supported path; do not claim image review based on a textual summary. Astra makes milestone M2 acceptance after inspecting evidence.

## C4 — delivery (Astra)

Depends C3 findings resolved and current checks. Integrate only accepted candidate to dedicated branch, update spec serially if a normative requirement changed, create draft PR with exact body via file, push fixes, observe actual CI and mark ready after gates. Apply repository declaration/merge rules. Never force-push unrelated work or deploy to an unconfigured destination. Record commit/PR URL, final CI states, capture hashes, review IDs, limitations and manual kitchen handoff. Milestone M3 is reviewable delivered UI change, with merge/distribution status stated accurately.

## Feedback and recovery

New user messages override proposed decisions. Before dispatch, persist feedback, classify affected requirement IDs, stop/rebase affected writers and invalidate stale evidence. Version material revisions; record bounded ordinary design decisions. Resume only with explicit consumption of revised inputs. Stop requested means stop new work and reconcile owned jobs immediately. Do not create a scheduled task or separate human-facing project/thread.
