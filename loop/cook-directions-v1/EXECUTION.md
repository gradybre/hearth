# Execution package — P-HEARTH-COOK-002 v1

Read PLAN.md as the normative contract. Record plan hashes and approval before author dispatch. Astra owns integration and acceptance; Claude supplies implementation and independent reviews. One source writer and one heavy job at a time, four model sessions maximum. No billed model fallback or background supervisor.

D0 — Astra readiness: reconcile repository changes and prior owned runs, read live controller capability gates/public contracts, check installed doctor. Isolate a feature branch/worktree based on current main; rebase the plan if cooking source changed. Record actual baseline and tool versions, then publish approved plan using supported interfaces. Do not revive stopped jobs or use bootstrap fixtures as product runs.

D1 — Claude Sonnet author: R1–R6, A1–A3. Allowed writes: lib/features/recipes/cook_along_screen.dart; new lib/features/recipes/cook_instruction_blocks.dart; new test/features/recipes/cook_instruction_blocks_test.dart; new test/features/recipes/cook_directions_layout_test.dart; new test/render/cook_directions_render_test.dart. Any existing test needing an expectation updated is proposed to Astra first. No write to ingredients, domain models, persistence, providers, global theme, dependencies or generated files. Astra alone updates spec and integration records. Inputs: approved plan/hash, exact files/interfaces, existing cooking tests and render helpers, relevant spec. Pure helper must preserve text/order and return display blocks, not steps; rendering retains source semantics. First return regression tests, prove failure, then source implementation. Inspect strict bundle paths/base hashes before applying. Output bounded to 18k tokens, 15-minute deadline, 4-minute no-progress bound, maximum two repair attempts; ambiguous boundaries return a question instead of relaxing preservation.

D2 — Astra validation: inspect diff, record actual candidate SHA/hash, run focused baseline plus new tests, format/analyze/full suite/gallery. Capture actual Flutter variants required by A1, including sentence/line-boundary cases and small text-scaled screens with timer tray. Native Mac synthetic fixture walkthrough. Observe commands by exit code, poll within 60 seconds; 30-minute full-suite deadline, terminate only owned jobs. No further heavy process concurrently.

D3 — independent Claude reviews: separate author, code/security and vision sessions, supported actual model IDs recorded. Code reviewer checks preservation/boundaries/semantics/state and approved scope; vision reviewer receives candidate captures including 24/26 comparison and all states, not descriptions in place of images. Read-only, 6k-token output limit, 10-minute deadline and 4-minute no-progress bound each. Astra records every finding/disposition, maximum two bounded repair/re-review cycles. Missing vision capability blocks visual acceptance, not silently substituted. User feedback invalidates affected evidence before more author work.

D4 — Astra release: update spec serially, PR with truthful required declarations and reviews/evidence, all checks green. User's established explicit review/merge/deploy authority permits merge despite documented negative regression tests; never falsify declarations. Build signed iOS release from merged source using existing config, deploy in place with devicectl to the confirmed same iPhone, verify launch and preserve install receipts. If device unavailable, report exact limitation and finish independent checks; do not delete app data or configure App Store distribution. Record plan/candidate/review/model/session/command/CI/deploy identities and evidence hashes. No promise of unsupervised continuation.
