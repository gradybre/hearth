# Photo-pair repair acceptance

Combine the panel and package-front photos in one read. A panel stating 2/3 cup (85 g) supplies both printed serving measures. The package-front net weight must not suppress the panel's gram measure. Only a blank draft's untouched, explicitly owned starter row is replaced. Existing serving values and identities survive; conflicting nutrition is held for review. A package-only request cannot supply nutrition, even when a model claims that it did. Unknown/legacy request roles remain compatible.

Independent Claude Opus review accepted code and security; Astra inspected the full findings. Required fraction-display and server-gate coverage findings were repaired with regressions. Empty package reads now keep the photos and offer retry; incomplete package information no longer triggers an unsolicited error. Save/reopen preserves every existing nutrient, including unknown versus stated zero.

Final review N2 disposition: reverted the unrelated candidate change to omit unclear major nutrient figures, restoring exactly the existing final label-prompt paragraph. This avoids broadening the repair into unknown-major-nutrient semantics. N1 wording and N3 contrived key collision accepted; other required/material findings closed.

Local validation: Flutter 3,878 passed, 86 skipped; gallery 64 passed; analysis clean; formatter unchanged. Deno full suite 144 passed / 95 steps; types and lint pass. Negative coverage includes forged provenance, blank/zero existing rows, conflicting portions, legacy roles, empty gated responses, prepared-basis/count disagreements, and save/reopen. Tests failed before their corresponding fixes.

Native synthetic fixture exercised both photo slots and package-only augmentation over existing nonzero nutrients. It writes to no household store; Save/reopen is verified separately through the real repository widget test. The supplied gallery shows package-review states, not the user's private photographs. Final deployed-model and installed-device verification is recorded separately after release; this document does not claim those runs are complete.

No schema, dependency, model, budget, authentication or URL-policy changes. No private photo files or credentials are included. Existing explicit user authority covers merge and installation after green CI.
